import { ImageNewStudentPipe } from './image-new-student.pipe';

describe('ImageNewStudentPipe', () => {
  it('create an instance', () => {
    const pipe = new ImageNewStudentPipe();
    expect(pipe).toBeTruthy();
  });
});
